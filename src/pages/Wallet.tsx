import { ArrowUpRight, Check, Copy, ShieldCheck } from 'lucide-react'
import { useWeb3 } from '../context/Web3Context'

export const Wallet = () => {
  const { isConnected, connectWallet, address, klcBalance, transactions, ownedNFTs } = useWeb3()
  if (!isConnected) return <main className="flex min-h-screen items-center justify-center bg-obsidian px-5 text-center"><div><p className="eyebrow justify-center">KALAA digital identity</p><h1 className="mt-6 font-display text-6xl text-ivory">Your collection,<br />verified.</h1><p className="mx-auto mt-6 max-w-md text-muted">Connect your wallet to access certificates, ownership records, and provenance history.</p><button type="button" onClick={connectWallet} className="quiet-button mt-8">Connect wallet</button></div></main>
  return <main className="min-h-screen bg-obsidian px-5 pb-24 pt-36 sm:px-10"><div className="mx-auto max-w-[1300px]"><div className="flex flex-col justify-between gap-6 border-b border-ivory/10 pb-10 sm:flex-row sm:items-end"><div><p className="eyebrow">Collector portal</p><h1 className="mt-6 font-display text-6xl text-ivory sm:text-8xl">Digital identity.</h1></div><p className="max-w-xs text-right text-xs text-muted">{address}<br /><span className="text-gold">{klcBalance.toLocaleString()} KLC available</span></p></div><div className="mt-12 grid gap-5 sm:grid-cols-3"><div className="border border-gold/30 bg-surface p-6"><ShieldCheck className="text-gold" size={20} /><p className="mt-8 text-[10px] uppercase tracking-[.16em] text-muted">Certificates</p><p className="mt-2 font-display text-4xl text-ivory">{ownedNFTs.length}</p></div><div className="border border-ivory/10 bg-surface p-6"><Check className="text-gold" size={20} /><p className="mt-8 text-[10px] uppercase tracking-[.16em] text-muted">Ownership</p><p className="mt-2 font-display text-4xl text-ivory">Verified</p></div><div className="border border-ivory/10 bg-surface p-6"><Copy className="text-gold" size={20} /><p className="mt-8 text-[10px] uppercase tracking-[.16em] text-muted">Transactions</p><p className="mt-2 font-display text-4xl text-ivory">{transactions.length}</p></div></div><section className="mt-20"><p className="eyebrow">Ownership records</p><h2 className="mt-5 font-display text-5xl text-ivory">Your verified works</h2>{ownedNFTs.length === 0 ? <p className="mt-8 text-muted">No ownership records yet.</p> : <div className="mt-8 divide-y divide-ivory/10 border-y border-ivory/10">{ownedNFTs.map((item) => <div key={item.tokenId} className="flex items-center justify-between py-5"><div><p className="font-display text-2xl text-ivory">{item.name || 'KALAA artwork'}</p><p className="mt-1 text-[10px] uppercase tracking-[.15em] text-muted">Certificate / {item.tokenId}</p></div><ArrowUpRight className="text-gold" size={16} /></div>)}</div>}</section></div></main>
}
